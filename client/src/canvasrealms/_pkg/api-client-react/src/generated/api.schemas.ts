/**
 * Generated by orval v8.5.3 🍺
 * Do not edit manually.
 * Api
 * Canvas Realms — spatial worldbuilding API
 * OpenAPI spec version: 0.1.0
 */
export interface HealthStatus {
  status: string;
}

export interface Realm {
  id: string;
  name: string;
  slug: string;
  /** @nullable */
  description?: string | null;
  /**
   * Accent color hex
   * @nullable
   */
  accent?: string | null;
  /** @nullable */
  ownerUserId?: string | null;
  /** True when this realm has a valid Arcana Adventure OAuth link. */
  arcanaLinked: boolean;
  /** @nullable */
  arcanaUserDisplay?: string | null;
  /**
   * Selected Arcana system, e.g. 'aa-v2'.
   * @nullable
   */
  arcanaSystem?: string | null;
  /**
   * Arcana host base URL the realm is linked to.
   * @nullable
   */
  arcanaHost?: string | null;
  createdAt: string;
  updatedAt: string;
}

export type RealmRole = (typeof RealmRole)[keyof typeof RealmRole];

export const RealmRole = {
  owner: "owner",
  editor: "editor",
  viewer: "viewer",
} as const;

export type CollaboratorRole =
  (typeof CollaboratorRole)[keyof typeof CollaboratorRole];

export const CollaboratorRole = {
  owner: "owner",
  editor: "editor",
  viewer: "viewer",
} as const;

export interface Collaborator {
  id: string;
  realmId: string;
  /** @nullable */
  userId?: string | null;
  role: CollaboratorRole;
  /** @nullable */
  invitedEmail?: string | null;
  /** @nullable */
  inviteToken?: string | null;
  invitedAt: string;
  /** @nullable */
  acceptedAt?: string | null;
}

export type InviteCollaboratorInputRole =
  (typeof InviteCollaboratorInputRole)[keyof typeof InviteCollaboratorInputRole];

export const InviteCollaboratorInputRole = {
  editor: "editor",
  viewer: "viewer",
} as const;

export interface InviteCollaboratorInput {
  email: string;
  role: InviteCollaboratorInputRole;
}

export type UpdateCollaboratorInputRole =
  (typeof UpdateCollaboratorInputRole)[keyof typeof UpdateCollaboratorInputRole];

export const UpdateCollaboratorInputRole = {
  editor: "editor",
  viewer: "viewer",
} as const;

export interface UpdateCollaboratorInput {
  role: UpdateCollaboratorInputRole;
}

export interface AcceptInviteInput {
  token: string;
}

export type AcceptInviteResultRole =
  (typeof AcceptInviteResultRole)[keyof typeof AcceptInviteResultRole];

export const AcceptInviteResultRole = {
  owner: "owner",
  editor: "editor",
  viewer: "viewer",
} as const;

export interface AcceptInviteResult {
  realmId: string;
  role: AcceptInviteResultRole;
  alreadyMember: boolean;
}

export interface RealmInput {
  /** @minLength 1 */
  name: string;
  description?: string;
  accent?: string;
  /**
   * Selected Arcana system, e.g. 'aa-v2'.
   * @nullable
   */
  arcanaSystem?: string | null;
}

export interface RealmUpdate {
  /** @minLength 1 */
  name?: string;
  description?: string;
  accent?: string;
  /**
   * Selected Arcana system, e.g. 'aa-v2'.
   * @nullable
   */
  arcanaSystem?: string | null;
}

export type NodeKind = (typeof NodeKind)[keyof typeof NodeKind];

export const NodeKind = {
  character: "character",
  location: "location",
  lore: "lore",
  faction: "faction",
  event: "event",
  item: "item",
  note: "note",
  canvas: "canvas",
  map: "map",
  spell: "spell",
  species: "species",
  class: "class",
  "feat-tree": "feat-tree",
  "character-template": "character-template",
  "roll-template": "roll-template",
  culture: "culture",
  religion: "religion",
  language: "language",
  "social-class": "social-class",
  region: "region",
  settlement: "settlement",
  building: "building",
  "ruin-or-dungeon": "ruin-or-dungeon",
  landmark: "landmark",
  "world-or-plane": "world-or-plane",
  geography: "geography",
  climate: "climate",
  biome: "biome",
  flora: "flora",
  fauna: "fauna",
  ecology: "ecology",
  plant: "plant",
  tree: "tree",
  "era-or-age": "era-or-age",
  "historical-event": "historical-event",
  "war-or-conflict": "war-or-conflict",
  "myth-or-legend": "myth-or-legend",
  prophecy: "prophecy",
  "magic-system": "magic-system",
  "artifact-or-relic": "artifact-or-relic",
  deity: "deity",
  "supernatural-entity": "supernatural-entity",
  ritual: "ritual",
  government: "government",
  "law-or-tradition": "law-or-tradition",
  "trade-good": "trade-good",
  currency: "currency",
  military: "military",
  "quest-or-plot-hook": "quest-or-plot-hook",
  timeline: "timeline",
  "lore-entry": "lore-entry",
  "secret-or-mystery": "secret-or-mystery",
} as const;

export type NodeMode = (typeof NodeMode)[keyof typeof NodeMode];

export const NodeMode = {
  window: "window",
  sticky: "sticky",
} as const;

export type NodeBlockType = (typeof NodeBlockType)[keyof typeof NodeBlockType];

export const NodeBlockType = {
  text: "text",
  media: "media",
  "map-ref": "map-ref",
  "canvas-ref": "canvas-ref",
} as const;

/**
 * For type=media|map-ref|canvas-ref. Display width: small (~50%), medium (~75%), full (100%).
 */
export type NodeBlockSize = (typeof NodeBlockSize)[keyof typeof NodeBlockSize];

export const NodeBlockSize = {
  small: "small",
  medium: "medium",
  full: "full",
} as const;

/**
 * A single field block on a node body.
 */
export interface NodeBlock {
  id: string;
  type: NodeBlockType;
  /** For type=text. Markdown content of this text block. */
  text?: string;
  /** For type=text. Optional H2 header rendered above the text. */
  heading?: string;
  /** For type=media. Image/file URL or objectPath. */
  url?: string;
  /** For type=media. Alt text. */
  alt?: string;
  /** For type=map-ref or canvas-ref. The referenced node id in the same realm. */
  nodeId?: string;
  /** For type=media|map-ref|canvas-ref. Display width: small (~50%), medium (~75%), full (100%). */
  size?: NodeBlockSize;
}

/**
 * Free-form Arcana Adventure stats payload, shape dictated by the AA OpenAPI schema for this kind.
 * @nullable
 */
export type NodeArcanaStats = { [key: string]: unknown } | null;

export interface Node {
  id: string;
  realmId: string;
  /** Short user-facing unique handle for this node within the realm. Auto-generated on create, editable by the user. */
  key: string;
  title: string;
  /** Rich text / markdown content (legacy single-textarea body). */
  content: string;
  tags: string[];
  kind: NodeKind;
  mode: NodeMode;
  minimized: boolean;
  x: number;
  y: number;
  width: number;
  height: number;
  zIndex: number;
  /** Accent color hex for the node */
  color: string;
  /**
   * Header portrait / pfp image URL or objectPath shown on the new node editor.
   * @nullable
   */
  imageUrl?: string | null;
  /** Ordered list of body field blocks (text/media/map-ref/canvas-ref) in the new structured editor. */
  blocks?: NodeBlock[];
  /**
   * Free-form Arcana Adventure stats payload, shape dictated by the AA OpenAPI schema for this kind.
   * @nullable
   */
  arcanaStats?: NodeArcanaStats;
  /** Whether changes to this node should be synced to Arcana Adventure (only meaningful for Arcana-mappable kinds). */
  arcanaSync?: boolean;
  /**
   * ID of the library folder this node lives in. Null means it sits at the realm root.
   * @nullable
   */
  folderId?: string | null;
  createdAt: string;
  updatedAt: string;
}

/**
 * @nullable
 */
export type NodeInputArcanaStats = { [key: string]: unknown } | null;

export interface NodeInput {
  /** @minLength 1 */
  title: string;
  /**
   * Optional explicit key. If omitted, the server generates one. Must be unique within the realm.
   * @minLength 1
   */
  key?: string;
  content?: string;
  tags?: string[];
  kind?: NodeKind;
  mode?: NodeMode;
  minimized?: boolean;
  x?: number;
  y?: number;
  width?: number;
  height?: number;
  zIndex?: number;
  color?: string;
  /** @nullable */
  imageUrl?: string | null;
  blocks?: NodeBlock[];
  /** @nullable */
  arcanaStats?: NodeInputArcanaStats;
  arcanaSync?: boolean;
  /** @nullable */
  folderId?: string | null;
}

/**
 * @nullable
 */
export type NodeUpdateArcanaStats = { [key: string]: unknown } | null;

export interface NodeUpdate {
  /** @minLength 1 */
  title?: string;
  /**
   * User-facing unique handle. Must be unique within the realm; server returns 409 with code key_conflict if not.
   * @minLength 1
   */
  key?: string;
  content?: string;
  tags?: string[];
  kind?: NodeKind;
  mode?: NodeMode;
  minimized?: boolean;
  x?: number;
  y?: number;
  width?: number;
  height?: number;
  zIndex?: number;
  color?: string;
  /** @nullable */
  imageUrl?: string | null;
  blocks?: NodeBlock[];
  /** @nullable */
  arcanaStats?: NodeUpdateArcanaStats;
  arcanaSync?: boolean;
  /** @nullable */
  folderId?: string | null;
  /** Move this node to a different realm. Caller must have editor access on the target realm. If folderId is not also provided it will be cleared. */
  realmId?: string;
}

export interface Folder {
  id: string;
  realmId: string;
  /**
   * Parent folder ID; null for top-level folders.
   * @nullable
   */
  parentFolderId?: string | null;
  name: string;
  sortIndex: number;
  createdAt: string;
  updatedAt: string;
}

export interface FolderInput {
  /** @minLength 1 */
  name: string;
  /** @nullable */
  parentFolderId?: string | null;
  sortIndex?: number;
}

export interface FolderUpdate {
  /** @minLength 1 */
  name?: string;
  /** @nullable */
  parentFolderId?: string | null;
  sortIndex?: number;
  /** Move this folder (and all of its descendants — subfolders and nodes) to a different realm. Caller must have editor access on the target realm. If parentFolderId is not also provided it will be cleared. */
  realmId?: string;
}

export interface Relationship {
  id: string;
  realmId: string;
  fromNodeId: string;
  toNodeId: string;
  /** @nullable */
  label?: string | null;
  createdAt: string;
}

export interface RelationshipInput {
  fromNodeId: string;
  toNodeId: string;
  label?: string;
}

export interface CanvasMember {
  id: string;
  canvasNodeId: string;
  memberNodeId: string;
  x: number;
  y: number;
  width: number;
  height: number;
  zIndex: number;
  createdAt: string;
}

export interface CanvasMemberInput {
  /** Optional explicit UUID. When provided, the server uses this id instead of allocating a new one. Used by client-side undo to restore a previously-deleted member with its original id. */
  id?: string;
  memberNodeId: string;
  x?: number;
  y?: number;
  width?: number;
  height?: number;
  zIndex?: number;
}

export interface CanvasMemberUpdate {
  x?: number;
  y?: number;
  width?: number;
  height?: number;
  zIndex?: number;
}

export interface Viewport {
  realmId: string;
  x: number;
  y: number;
  zoom: number;
  updatedAt: string;
}

export interface ViewportInput {
  x: number;
  y: number;
  zoom: number;
}

export interface TagCount {
  tag: string;
  count: number;
}

export type RealmSummaryKindCountsItem = {
  kind: NodeKind;
  count: number;
};

export interface RealmSummary {
  realmId: string;
  nodeCount: number;
  relationshipCount: number;
  kindCounts: RealmSummaryKindCountsItem[];
  /** @nullable */
  lastUpdated: string | null;
}

export interface UploadUrlRequest {
  /** @minLength 1 */
  name: string;
  /** @minimum 1 */
  size: number;
  /** @minLength 1 */
  contentType: string;
}

export interface ArcanaAuthorizeUrl {
  authorizeUrl: string;
  state: string;
}

export interface UploadUrlResponse {
  uploadURL: string;
  objectPath: string;
}

export type WikiTheme = (typeof WikiTheme)[keyof typeof WikiTheme];

export const WikiTheme = {
  light: "light",
  dark: "dark",
  auto: "auto",
} as const;

export type WikiEntrySize = (typeof WikiEntrySize)[keyof typeof WikiEntrySize];

export const WikiEntrySize = {
  small: "small",
  medium: "medium",
  large: "large",
  full: "full",
} as const;

export interface WikiEntryShow {
  title: boolean;
  summary: boolean;
  content: boolean;
  image: boolean;
}

export interface WikiEntryPosition {
  x: number;
  y: number;
  w: number;
  h: number;
}

export interface WikiEntry {
  nodeId: string;
  order: number;
  /** @nullable */
  sectionId: string | null;
  size: WikiEntrySize;
  show: WikiEntryShow;
  position?: WikiEntryPosition | null;
}

export interface WikiSection {
  id: string;
  title: string;
}

export interface WikiDraft {
  title: string;
  /** @nullable */
  tagline?: string | null;
  /** @nullable */
  coverImage?: string | null;
  theme: WikiTheme;
  showSidebar: boolean;
  /** When true, public renderer honors per-entry x/y/w/h positions instead of section-grouped flow. */
  freeLayout?: boolean;
  sections: WikiSection[];
  entries: WikiEntry[];
}

export type WikiDraftInput = WikiDraft;

export interface WikiSnapshotSummary {
  id: string;
  publishedAt: string;
  /** @nullable */
  publishedByUserId?: string | null;
  isLive: boolean;
  title: string;
}

export interface WikiDraftState {
  realmId: string;
  slug: string;
  draft: WikiDraft;
  hasUnpublishedChanges: boolean;
  isPublished: boolean;
  /** @nullable */
  publishedSnapshotId?: string | null;
  /** @nullable */
  publishedAt?: string | null;
}

export interface WikiPublishedNode {
  id: string;
  title: string;
  kind: string;
  content: string;
  color: string;
  tags: string[];
  /** @nullable */
  coverImage?: string | null;
}

export interface WikiPublishedEntry {
  nodeId: string;
  order: number;
  /** @nullable */
  sectionId: string | null;
  size: WikiEntrySize;
  show: WikiEntryShow;
  position?: WikiEntryPosition | null;
  node: WikiPublishedNode;
}

export interface PublicWiki {
  realmId: string;
  slug: string;
  publishedAt: string;
  layout: WikiDraft;
  entries: WikiPublishedEntry[];
}

export type GetArcanaOpenapi200 = { [key: string]: unknown };
